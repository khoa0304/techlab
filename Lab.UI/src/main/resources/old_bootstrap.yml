eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:18761/eureka}
spring:
  application:
    name:ui-service
  cloud:
    config:
      uri:http://khoa:welcome123@localhost:11512
      name:config-service
      profile:native
      enabled:true      
      fail-fast:true  