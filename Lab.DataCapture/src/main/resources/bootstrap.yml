eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://discovery:18761/eureka}
spring:
  application:
    name: datacapture-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIGSERVER
      failFast: true